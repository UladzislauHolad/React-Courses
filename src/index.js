(function(){
    'use strict'
    let composer = new Composer();
    composer.compose(document.getElementById('container'));

    // console.log(BrandsService.getData());
    // console.log(ModelsService.getData());
    // console.log(ModelsService.getData('BMW'));
    // console.log(TransmissionTypesService.getData());
    // console.log(FuelTypesService.getData());

    // let tab = new WizardTab('div', 'tab', new BrandsService());
    // tab.init();
})();